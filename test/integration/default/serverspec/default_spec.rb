require 'spec_helper'

describe 'pelican_blog::default' do

  # Serverspec examples can be found at
  # http://serverspec.org/resource_types.html
  # it 'provides a working nginx server listening on port 80' do
  #   describe port(80) do
  #     it { should be_listening }
  #   end
  # end
  # it 'installs configured and runs nginx' do
  #   describe service('nginx'), :if => os[:family] == 'redhat' do
  #     it { should be_enabled }
  #     it { should be_running }
  #   end
  # end
end
